We have applied Pearson Correlation Coefficient for feature selection and then have divided the dataset into 2 parts.
One Part is used as training set. Other part is used as Testing set.
60 instances in total. 40 instances for training and 20 instances for testing.
